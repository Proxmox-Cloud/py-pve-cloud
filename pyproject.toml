[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "py-pve-cloud"
authors = [{ name = "Tobias Huebner", email = "tobias.huebner@vmzberlin.com" }]
license = "GPL-3.0-or-later"
license-files = ["LICENSE.md"]
dependencies = [
  "psycopg2-binary==2.9.11",
  "SQLAlchemy==2.0.45",
  "alembic==1.18.0",
  "paramiko==4.0.0",
  "proxmoxer==2.2.0",
  "dnspython==2.8.0",
  "rpyc==6.0.2",
  "fabric==3.2.3",
  "asyncssh==2.22.0",
  "pve-cloud-schemas>=3.1.6,<3.2.0"
]
dynamic = ["version"]

[tool.setuptools.dynamic]
version = {attr = "pve_cloud._version.__version__"}

[tool.setuptools.package-data]
"pve_cloud.orm" = ["*.mako", "*.ini"]

[project.scripts]
pvclu = "pve_cloud.cli.pvclu:main"
pvcli = "pve_cloud.cli.pvcli:main"
pxrpc = "pve_cloud.cli.pxrpc:main"


